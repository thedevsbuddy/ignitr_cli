name: ignitr_cli
description: "A cli tool to work with Ignitr Project."
version: 0.1.3
homepage: https://github.com/thedevsbuddy/ignitr_cli

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  archive: ^4.0.9
  args: ^2.6.0
  dcli: ^8.4.2
  flutter:
    sdk: flutter
  http: ^1.3.0
  path: ^1.9.0
  pluralize: ^0.0.8
  recase: ^4.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# flutter:

executables:
  ignitr: main
